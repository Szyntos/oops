query SetupAwards {
  award(orderBy: { awardName: ASC }) {
    awardEditions {
      editionId
    }
    awardId
    awardName
    awardType
    awardValue
    description
    imageFileId
    label
    maxUsages
    categoryId
  }
}
