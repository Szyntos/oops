query CurrentUser {
  getCurrentUser {
    editions {
      editionName
      editionId
      editionYear
      endDate
      label
      startDate
    }
    user {
      nick
      role
      userId
      avatarSetByUser
      nickSetByUser
    }
  }
}
