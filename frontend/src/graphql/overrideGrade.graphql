mutation OverrideGrade($editionId: Int!, $grade: Float!, $userId: Int!) {
  overrideComputedGradeForUser(
    editionId: $editionId
    grade: $grade
    userId: $userId
  ) {
    computedGrade
  }
}
