mutation SetupGroupEdit(
  $groupId: Int!
  $startTime: String!
  $endTime: String!
  $usosId: Int!
  $weekdayId: Int!
  $teacherId: Int!
) {
  editGroup(
    groupId: $groupId
    endTime: $endTime
    startTime: $startTime
    teacherId: $teacherId
    usosId: $usosId
    weekdayId: $weekdayId
  ) {
    usosId
  }
}
