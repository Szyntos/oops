query CurrentUser {
  getCurrentUser {
    nick
    role
    userId
    avatarSetByUser
    nickSetByUser
    userGroups {
      group {
        edition {
          editionId
          editionName
          editionYear
          endDate
          label
          startDate
        }
      }
    }
  }
}
